server:
  port: 9299
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/factory_users
    username: compose-postgres
    password: compose-postgres
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none